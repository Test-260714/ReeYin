<UserControl x:Class="HardwareTool.Motion.Views.DelayMotionOperationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="280"
             d:DesignWidth="850">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#F8F9FA"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                CornerRadius="6"
                Padding="20 12"
                Margin="0 0 0 15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="延时动作"
                           FontWeight="SemiBold"
                           Foreground="#555555"
                           VerticalAlignment="Center"
                           FontSize="14"/>
                <TextBlock Text=" - 当前轨迹会在执行流程中等待设定时间。"
                           Foreground="#888888"
                           VerticalAlignment="Center"
                           FontSize="12"
                           Margin="5 0 0 0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                Background="White"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                CornerRadius="6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="{DynamicResource PrimaryBrush}"
                        CornerRadius="6 6 0 0"
                        Padding="12 8">
                    <TextBlock Text="延时参数"
                               Foreground="White"
                               FontWeight="SemiBold"
                               FontSize="13"/>
                </Border>

                <StackPanel Grid.Row="1"
                            Margin="20">
                    <TextBlock Text="延时时间 (s)"
                               Foreground="#555555"
                               FontWeight="Medium"
                               Margin="0 0 0 5"/>
                    <hc:NumericUpDown Minimum="0"
                                      Maximum="1000"
                                      Style="{StaticResource NumericUpDownExtendGlobal}"
                                      Increment="1"
                                      Width="240"
                                      Value="{Binding ModelParam.SltMovementLocus.OutputIODelay, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="说明：这里沿用原模块中的延时字段，便于与现有执行逻辑兼容。"
                               Foreground="#888888"
                               Margin="0 15 0 0"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>

