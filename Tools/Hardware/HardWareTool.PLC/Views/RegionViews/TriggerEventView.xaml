<UserControl x:Class="HardWareTool.PLC.Views.TriggerEventView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="280" d:DesignWidth="850">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 说明卡片 -->
        <Border Grid.Row="0" Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="6" Padding="20 12" Margin="0 0 0 15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="触发事件" FontWeight="SemiBold" Foreground="#555555" VerticalAlignment="Center" FontSize="14"/>
                <TextBlock Text=" — 触发指定名称的系统事件" Foreground="#888888" VerticalAlignment="Center" FontSize="12" Margin="5 0 0 0"/>
            </StackPanel>
        </Border>

        <!-- 设置卡片 -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{DynamicResource PrimaryBrush}" CornerRadius="6 6 0 0" Padding="12 8">
                    <TextBlock Text="事件设置" Foreground="White" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Row="1" Margin="20">
                    <TextBlock Text="事件名称" Foreground="#555555" FontWeight="Medium" Margin="0 0 0 5"/>
                    <TextBox Style="{StaticResource TextBoxExtend}"
                             Text="{Binding ModelParam.SltPLCOrder.SltEventName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                    <Border Background="#FFF3E0" BorderBrush="#FFCC80" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0 15 0 0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="提示：" Foreground="#E65100" FontWeight="Medium" Margin="0 0 5 0" VerticalAlignment="Center"/>
                            <TextBlock Text="确保事件名称与系统中注册的事件一致" Foreground="#E65100" FontSize="12" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
