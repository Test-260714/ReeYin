<UserControl x:Class="HardWareTool.PLC.Views.DelayOperationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="280" d:DesignWidth="850">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 说明卡片 -->
        <Border Grid.Row="0" Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="6" Padding="20 12" Margin="0 0 0 15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="延时操作" FontWeight="SemiBold" Foreground="#555555" VerticalAlignment="Center" FontSize="14"/>
                <TextBlock Text=" - 暂停执行指定时间后继续" Foreground="#888888" VerticalAlignment="Center" FontSize="12" Margin="5 0 0 0"/>
            </StackPanel>
        </Border>

        <!-- 设置卡片 -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{DynamicResource PrimaryBrush}" CornerRadius="6 6 0 0" Padding="12 8">
                    <TextBlock Text="延时设置" Foreground="White" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Row="1" Margin="20">
                    <TextBlock Text="设定时间（ms）" Foreground="#555555" FontWeight="Medium" Margin="0 0 0 5"/>
                    <hc:NumericUpDown Style="{StaticResource NumericUpDownExtendGlobal}" Increment="100" Maximum="60000" Minimum="0"
                                      Value="{Binding ModelParam.SltPLCOrder.WaitDelay, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                    <Border Background="#F5F5F5" CornerRadius="4" Padding="10" Margin="0 15 0 0">
                        <StackPanel>
                            <TextBlock Text="换算提示" Foreground="#888888" FontSize="11" Margin="0 0 0 5"/>
                            <TextBlock Foreground="#555555" FontSize="12">
                                <Run Text="1000 ms = 1 秒"/>
                                <Run Text="60000 ms = 1 分钟"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>

