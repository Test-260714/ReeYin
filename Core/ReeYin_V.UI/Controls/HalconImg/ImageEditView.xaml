<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:control="clr-namespace:ReeYin_V.UI.Controls"
                    xmlns:halcon="clr-namespace:HalconDotNet;assembly=halcondotnetxl"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <DrawingBrush
        x:Key="ImageEditViewCheckerboardBrush"
        Stretch="None"
        TileMode="Tile"
        Viewbox="0,0,24,24"
        ViewboxUnits="Absolute"
        Viewport="0,0,24,24"
        ViewportUnits="Absolute">
        <DrawingBrush.Drawing>
            <DrawingGroup>
                <GeometryDrawing Brush="White">
                    <GeometryDrawing.Geometry>
                        <RectangleGeometry Rect="0,0,24,24" />
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
                <GeometryDrawing Brush="Black">
                    <GeometryDrawing.Geometry>
                        <GeometryGroup>
                            <RectangleGeometry Rect="0,0,12,12" />
                            <RectangleGeometry Rect="12,12,12,12" />
                        </GeometryGroup>
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
            </DrawingGroup>
        </DrawingBrush.Drawing>
    </DrawingBrush>

    <Style TargetType="control:VMHWindowControl">
        <Setter Property="Background" Value="#202020" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="control:VMHWindowControl">
                    <Border Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                        <Grid ClipToBounds="True">
                            <Rectangle Fill="{TemplateBinding Background}" />
                            <halcon:HSmartWindowControlWPF
                                x:Name="PART_SMART"
                                Background="{TemplateBinding Background}" />

                            <TextBlock
                                x:Name="PART_MSG"
                                Margin="8"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Top"
                                Panel.ZIndex="1"
                                FontSize="14"
                                FontWeight="Bold"
                                Foreground="#FFEA4335"
                                IsHitTestVisible="False" />

                            <Border
                                x:Name="PART_STATUS_BAR"
                                Margin="8"
                                Padding="10,6"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Bottom"
                                Panel.ZIndex="1"
                                Background="#99000000"
                                BorderBrush="#33FFFFFF"
                                BorderThickness="1"
                                CornerRadius="4"
                                Visibility="Collapsed"
                                IsHitTestVisible="False">
                                <TextBlock
                                    x:Name="PART_STATUS"
                                    FontSize="12"
                                    Foreground="White"
                                    TextWrapping="NoWrap" />
                            </Border>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="control:ImageEditView" BasedOn="{StaticResource {x:Type control:VMHWindowControl}}">
        <Setter Property="Background" Value="{StaticResource ImageEditViewCheckerboardBrush}" />
        <Setter Property="IsShapeDrawingEnabled" Value="False" />
        <Setter Property="IsStatusBarVisible" Value="True" />
    </Style>
</ResourceDictionary>
