<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:cx="clr-namespace:ReeYin_V.UI.UserControls.WxSearchBox"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/ReeYin_V.UI;component/Style/FontAwesome.xaml"/>
        <ResourceDictionary Source="pack://application:,,,/ReeYin_V.UI;component/Style/Converter.xaml"/>
        <ResourceDictionary Source="pack://application:,,,/ReeYin_V.UI;component/Style/Button.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    <Style TargetType="{x:Type cx:WxSearchBox}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="FontSize" Value="{DynamicResource SubTitleFontSize}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="2"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="IconSize" Value="18"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type cx:WxSearchBox}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Background="{TemplateBinding TitleBackground}" TextAlignment="Left" Padding="{TemplateBinding Padding}"
                                   IsHitTestVisible="False" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Foreground="{TemplateBinding Foreground}"
                                   FontSize="{TemplateBinding FontSize}" Width="{TemplateBinding TitleWidth}" Text="{TemplateBinding Title}"
                                   Visibility="Visible"/>

                        <Border x:Name="border" Grid.Column="1" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{TemplateBinding CornerRadius}" SnapsToDevicePixels="True"/>

                        <Grid Grid.Column="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            <!-- 文本内容区域 -->
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                            <!-- 水印 -->
                            <TextBlock x:Name="watermark" Text="{TemplateBinding WaterMark}" Background="Transparent" IsHitTestVisible="False" 
                                       Margin="{TemplateBinding Padding}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                                       HorizontalAlignment="Stretch" Foreground="White"
                                       Visibility="{TemplateBinding WaterMark,Converter={StaticResource EmptyTextToVisibilityConverter}}"/>
                            <!-- 搜索按钮-->
                            <Button x:Name="PART_ButtonSearch" Height="30" Width="30" FontFamily="{StaticResource Iconfont}" Content="&#xe8b9;" Style="{StaticResource GeneralButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0"
                                Command="cx:ControlCommands.Search" IsEnabled="{Binding IsEnabled}"/>
                        </Grid>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="true">
                            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="HasText" Value="true">
                            <Setter TargetName="watermark" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger SourceName="PART_ButtonSearch" Property="IsMouseOver" Value="true">
                            <Setter TargetName="PART_ButtonSearch" Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.7"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
