<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:cx="clr-namespace:ReeYin_V.UI.UserControls.WxGroupBox"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--#region WxGroupBox -->
    <Style TargetType="{x:Type cx:WxGroupBox}">
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
        <Setter Property="HeaderForeground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="BorderBrush" Value="{DynamicResource MainTopSepBrush}"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="FontSize" Value="{DynamicResource DefaultFontSize}"/>
        <Setter Property="CornerRadius" Value="6 6 0 0"/>
        <Setter Property="TitlePosition" Value="Top"/>
        <Style.Triggers>

            <!-- Top 布局 -->
            <Trigger Property="TitlePosition" Value="Top">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type cx:WxGroupBox}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>

                                <!-- 标题栏 -->
                                <Border Background="{TemplateBinding Background}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        CornerRadius="{TemplateBinding CornerRadius}"
                                        Padding="3,3">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition/>
                                        </Grid.ColumnDefinitions>

                                        <!-- 左侧竖条装饰 -->
                                        <Border Width="3" CornerRadius="1.5"
                                                Background="{TemplateBinding HeaderForeground}"
                                                VerticalAlignment="Stretch" Margin="0,2,8,2"/>

                                        <!-- 图标 -->
                                        <TextBlock Grid.Column="1"
                                                   Text="{TemplateBinding Icon}"
                                                   FontFamily="{DynamicResource Iconfont}"
                                                   FontSize="{TemplateBinding IconSize}"
                                                   Foreground="{TemplateBinding HeaderForeground}"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,6,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <Trigger Property="Text" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </Trigger>
                                                        <Trigger Property="Text" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>

                                        <!-- 标题文字 -->
                                        <ContentPresenter Grid.Column="2"
                                                          ContentSource="Header"
                                                          RecognizesAccessKey="True"
                                                          VerticalAlignment="Center"
                                                          TextBlock.Foreground="{TemplateBinding HeaderForeground}"
                                                          TextBlock.FontWeight="DemiBold"/>
                                    </Grid>
                                </Border>

                                <!-- 内容区域 -->
                                <ContentPresenter Grid.Row="1"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>

            <!-- Left 布局 -->
            <Trigger Property="TitlePosition" Value="Left">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type cx:WxGroupBox}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>

                                <!-- 标题栏（竖向） -->
                                <Border Background="{TemplateBinding Background}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        CornerRadius="{TemplateBinding CornerRadius}"
                                        Padding="8,12">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition/>
                                        </Grid.RowDefinitions>

                                        <!-- 顶部横条装饰 -->
                                        <Border Height="3" CornerRadius="1.5"
                                                Background="{TemplateBinding HeaderForeground}"
                                                HorizontalAlignment="Stretch" Margin="2,0,2,8"/>

                                        <!-- 图标 -->
                                        <TextBlock Grid.Row="1"
                                                   Text="{TemplateBinding Icon}"
                                                   FontFamily="{DynamicResource Iconfont}"
                                                   FontSize="{TemplateBinding IconSize}"
                                                   Foreground="{TemplateBinding HeaderForeground}"
                                                   HorizontalAlignment="Center"
                                                   Margin="0,0,0,6">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <Trigger Property="Text" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </Trigger>
                                                        <Trigger Property="Text" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>

                                        <!-- 分隔线 -->
                                        <Border Grid.Row="2" Height="1" Margin="4,0,4,6"
                                                Background="{TemplateBinding HeaderForeground}" Opacity="0.3"/>

                                        <!-- 标题文字（竖排） -->
                                        <ContentPresenter Grid.Row="3"
                                                          ContentSource="Header"
                                                          RecognizesAccessKey="True"
                                                          VerticalAlignment="Top"
                                                          HorizontalAlignment="Center"
                                                          TextBlock.Foreground="{TemplateBinding HeaderForeground}"
                                                          TextBlock.FontWeight="DemiBold">
                                            <ContentPresenter.LayoutTransform>
                                                <RotateTransform Angle="-90"/>
                                            </ContentPresenter.LayoutTransform>
                                        </ContentPresenter>
                                    </Grid>
                                </Border>

                                <!-- 内容区域 -->
                                <ContentPresenter Grid.Column="1"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>

            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.7" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <!--#endregion-->
</ResourceDictionary>
