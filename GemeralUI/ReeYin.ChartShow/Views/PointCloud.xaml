<UserControl x:Class="ReeYin.ChartShow.Views.PointCloud"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Chart="clr-namespace:ReeYin_V.UI.UserControls.PointCloudDisplay;assembly=ReeYin_V.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Border>
            <Chart:PointCloudDisplayView x:Name="pointCloudDisplay">

            </Chart:PointCloudDisplayView>
        </Border>
        <Grid Grid.Row="1">
            <Button Content="加载文件" Margin="10 0 0 0" Height="auto" Width="auto" FontSize="12" HorizontalAlignment="Right"
                                   Click="LoadFileButton_Click"/>
        </Grid>
    </Grid>
</UserControl>
