<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:lc="clr-namespace:Arction.Wpf.ChartingMVVM;assembly=Arction.Wpf.ChartingMVVM.LightningChart"
                    xmlns:models="clr-namespace:ReeYin.AlarmCenter.Models">

    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

    <SolidColorBrush x:Key="AlarmBorderBrush" Color="{DynamicResource MainBorderColor}" />
    <SolidColorBrush x:Key="AlarmTextBrush" Color="{DynamicResource TextColor}" />
    <SolidColorBrush x:Key="AlarmMutedBrush" Color="{DynamicResource MainPrimaryTextIconColor}" />
    <SolidColorBrush x:Key="AlarmPanelBrush" Color="{DynamicResource RegionBackgroundColor}" />
    <SolidColorBrush x:Key="AlarmPrimaryBrush" Color="{DynamicResource PrimaryColor}" />
    <SolidColorBrush x:Key="AlarmShellBackgroundBrush" Color="{DynamicResource MainBackgroundColor}" />
    <SolidColorBrush x:Key="AlarmRailBackgroundBrush" Color="{DynamicResource RegionBackgroundColor}" />
    <SolidColorBrush x:Key="AlarmRailBorderBrush" Color="{DynamicResource MainBorderColor}" />
    <SolidColorBrush x:Key="AlarmRailSelectedBrush" Color="{DynamicResource SelectedColor}" />
    <SolidColorBrush x:Key="AlarmRailCardBrush" Color="{DynamicResource RegionBackgroundColor}" />
    <SolidColorBrush x:Key="AlarmRailMutedBrush" Color="{DynamicResource MainPrimaryTextIconColor}" />
    <SolidColorBrush x:Key="AlarmCyanBrush" Color="{DynamicResource SecondaryColor}" />
    <SolidColorBrush x:Key="AlarmDangerBrush" Color="#DC2626" />
    <SolidColorBrush x:Key="AlarmWarningBrush" Color="#F59E0B" />
    <SolidColorBrush x:Key="AlarmSuccessBrush" Color="#16A34A" />
    <SolidColorBrush x:Key="AlarmDangerSoftBrush" Color="#FEF2F2" />
    <SolidColorBrush x:Key="AlarmWarningSoftBrush" Color="#FFFBEB" />
    <SolidColorBrush x:Key="AlarmSuccessSoftBrush" Color="#ECFDF3" />
    <SolidColorBrush x:Key="AlarmCardHighlightBrush" Color="{DynamicResource PrimaryColor}" Opacity="0.08" />
    <SolidColorBrush x:Key="AlarmPrimarySoftBrush" Color="{DynamicResource PrimaryColor}" Opacity="0.12" />
    <SolidColorBrush x:Key="AlarmRailHoverBrush" Color="{DynamicResource PrimaryColor}" Opacity="0.08" />
    <SolidColorBrush x:Key="AlarmRailPressedBrush" Color="{DynamicResource PrimaryColor}" Opacity="0.16" />
    <SolidColorBrush x:Key="AlarmSelectedTextBrush" Color="{DynamicResource TitleForegroundColor}" />
    <SolidColorBrush x:Key="AlarmSelectedMutedBrush" Color="{DynamicResource TitleForegroundColor}" Opacity="0.82" />
    <SolidColorBrush x:Key="AlarmOnAccentBrush" Color="{DynamicResource TitleForegroundColor}" />
    <SolidColorBrush x:Key="AlarmAlternateRowBrush" Color="{DynamicResource MainBackgroundColor}" />
    <SolidColorBrush x:Key="AlarmFallbackBadgeBrush" Color="{DynamicResource MainPrimaryTextIconColor}" />
    <SolidColorBrush x:Key="AlarmFallbackSoftBrush" Color="{DynamicResource MainBackgroundColor}" />
    <SolidColorBrush x:Key="AlarmModalOverlayBrush" Color="{DynamicResource TextColor}" Opacity="0.40" />

    <LinearGradientBrush x:Key="AlarmRailBackgroundGradient" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="{DynamicResource RegionBackgroundColor}" Offset="0" />
        <GradientStop Color="{DynamicResource MainBackgroundColor}" Offset="0.55" />
        <GradientStop Color="{DynamicResource RegionBackgroundColor}" Offset="1" />
    </LinearGradientBrush>

    <Style x:Key="AlarmShellRootStyle" TargetType="Grid">
        <Setter Property="Margin" Value="0" />
        <Setter Property="Background" Value="{StaticResource AlarmShellBackgroundBrush}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <Style x:Key="CardStyle" TargetType="Border">
        <Setter Property="Padding" Value="16" />
        <Setter Property="Background" Value="{DynamicResource RegionBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource AlarmBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <Style x:Key="AlarmContentCardStyle" TargetType="Border" BasedOn="{StaticResource CardStyle}">
        <Setter Property="Padding" Value="18" />
        <Setter Property="Background" Value="{DynamicResource RegionBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource AlarmBorderBrush}" />
        <Setter Property="CornerRadius" Value="18" />
    </Style>

    <Style x:Key="AlarmCommandBarStyle" TargetType="Border" BasedOn="{StaticResource AlarmContentCardStyle}">
        <Setter Property="Padding" Value="14" />
        <Setter Property="Background" Value="{DynamicResource RegionBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource MainTopSepBrush}" />
    </Style>

    <Style x:Key="AlarmRailStyle" TargetType="Border">
        <Setter Property="Padding" Value="14" />
        <Setter Property="Background" Value="{StaticResource AlarmRailBackgroundGradient}" />
        <Setter Property="BorderBrush" Value="{StaticResource AlarmRailBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="24" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <Style x:Key="QueryBarStyle" TargetType="Border" BasedOn="{StaticResource CardStyle}">
        <Setter Property="Padding" Value="14" />
        <Setter Property="Background" Value="{DynamicResource RegionBackgroundBrush}" />
    </Style>

    <Style x:Key="AlarmWorkbenchNavigationPanel" TargetType="Border" BasedOn="{StaticResource CardStyle}">
        <Setter Property="Padding" Value="12" />
        <Setter Property="Background" Value="{DynamicResource RegionBackgroundBrush}" />
    </Style>

    <DataTemplate x:Key="AlarmPageNavButtonContentTemplate" DataType="{x:Type models:AlarmWorkbenchPageItem}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Border x:Name="NavCodeBadge"
                    Width="34"
                    Height="34"
                    Background="{StaticResource AlarmPrimarySoftBrush}"
                    BorderBrush="{StaticResource AlarmRailBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="12">
                <TextBlock x:Name="NavCodeText"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="11"
                           FontWeight="Bold"
                           Foreground="{StaticResource AlarmPrimaryBrush}"
                           Text="{Binding NavigationCode}" />
            </Border>
            <TextBlock x:Name="NavHeaderText"
                       Grid.Column="1"
                       Margin="10,0,10,0"
                       VerticalAlignment="Center"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource AlarmTextBrush}"
                       Text="{Binding Header}"
                       TextTrimming="CharacterEllipsis" />
            <Border x:Name="NavStatusDot"
                    Grid.Column="2"
                    Width="8"
                    Height="8"
                    VerticalAlignment="Center"
                    Background="{StaticResource AlarmRailBorderBrush}"
                    CornerRadius="4" />
        </Grid>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                <Setter TargetName="NavHeaderText" Property="Foreground" Value="{StaticResource AlarmPrimaryBrush}" />
                <Setter TargetName="NavCodeBadge" Property="Background" Value="{StaticResource AlarmCyanBrush}" />
                <Setter TargetName="NavCodeBadge" Property="BorderBrush" Value="{StaticResource AlarmCyanBrush}" />
                <Setter TargetName="NavCodeText" Property="Foreground" Value="{StaticResource AlarmOnAccentBrush}" />
                <Setter TargetName="NavStatusDot" Property="Background" Value="{StaticResource AlarmCyanBrush}" />
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <Style x:Key="AlarmPageNavButtonStyle" TargetType="Button" BasedOn="{StaticResource GeneralButtonStyle}">
        <Setter Property="MinHeight" Value="54" />
        <Setter Property="Margin" Value="0,0,0,8" />
        <Setter Property="Padding" Value="10,8" />
        <Setter Property="ContentTemplate" Value="{StaticResource AlarmPageNavButtonContentTemplate}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{StaticResource AlarmTextBrush}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="RootBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="18"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border x:Name="SelectionRail" Background="Transparent" CornerRadius="18,0,0,18" />
                            <ContentPresenter Grid.Column="1"
                                              Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{StaticResource AlarmRailHoverBrush}" />
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{StaticResource AlarmRailSelectedBrush}" />
                            <Setter TargetName="SelectionRail" Property="Background" Value="{StaticResource AlarmCyanBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{StaticResource AlarmRailPressedBrush}" />
                        </Trigger>
                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                            <Setter TargetName="SelectionRail" Property="Background" Value="{StaticResource AlarmCyanBrush}" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                <Setter Property="Background" Value="{StaticResource AlarmRailHoverBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource AlarmCyanBrush}" />
                <Setter Property="Foreground" Value="{StaticResource AlarmPrimaryBrush}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="AlarmPrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource GeneralButtonStyle}">
        <Setter Property="Height" Value="34" />
        <Setter Property="MinWidth" Value="92" />
        <Setter Property="Padding" Value="14,0" />
        <Setter Property="Background" Value="{DynamicResource PrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource SelectedBrush}" />
        <Setter Property="Foreground" Value="{StaticResource AlarmOnAccentBrush}" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="AlarmSecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource GeneralButtonStyle}">
        <Setter Property="Height" Value="34" />
        <Setter Property="MinWidth" Value="92" />
        <Setter Property="Padding" Value="14,0" />
        <Setter Property="Background" Value="{DynamicResource RegionBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource MainTopSepBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource AlarmTextBrush}" />
    </Style>

    <Style x:Key="MutedTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource AlarmMutedBrush}" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style x:Key="AlarmPageIntroTextStyle" TargetType="TextBlock" BasedOn="{StaticResource MutedTextStyle}">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="LineHeight" Value="20" />
    </Style>

    <Style x:Key="SmallLabelStyle" TargetType="TextBlock" BasedOn="{StaticResource MutedTextStyle}">
        <Setter Property="FontSize" Value="11" />
        <Setter Property="Margin" Value="0,0,0,6" />
    </Style>

    <Style x:Key="FlatColumnHeaderStyle" TargetType="DataGridColumnHeader">
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="TextBlock.TextAlignment" Value="Center" />
        <Setter Property="Background" Value="{DynamicResource RegionBackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="BorderBrush" Value="{DynamicResource MainTopSepBrush}" />
        <Setter Property="Padding" Value="8,8" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="FlatCellStyle" TargetType="DataGridCell">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=DataGridRow}}" />
        <Setter Property="Padding" Value="6,4" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="TextBlock.TextAlignment" Value="Center" />
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource SelectedBrush}" />
                <Setter Property="Foreground" Value="{StaticResource AlarmOnAccentBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource SelectedBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="CenteredDataGridTextElementStyle" TargetType="TextBlock">
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="TextAlignment" Value="Center" />
    </Style>

    <Style x:Key="FlatRowStyle" TargetType="DataGridRow">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <Setter Property="MinHeight" Value="38" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource AlarmPrimarySoftBrush}" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource SelectedBrush}" />
                <Setter Property="Foreground" Value="{StaticResource AlarmOnAccentBrush}" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="AlarmDataGridStyle" TargetType="DataGrid" BasedOn="{StaticResource DefaultDataGridStyle}">
        <Setter Property="Background" Value="{DynamicResource RegionBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource AlarmBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="ColumnHeaderStyle" Value="{StaticResource FlatColumnHeaderStyle}" />
        <Setter Property="CellStyle" Value="{StaticResource FlatCellStyle}" />
        <Setter Property="RowStyle" Value="{StaticResource FlatRowStyle}" />
        <Setter Property="GridLinesVisibility" Value="None" />
        <Setter Property="HeadersVisibility" Value="Column" />
        <Setter Property="RowHeaderWidth" Value="0" />
        <Setter Property="CanUserAddRows" Value="False" />
        <Setter Property="CanUserDeleteRows" Value="False" />
        <Setter Property="AutoGenerateColumns" Value="False" />
    </Style>

    <Style x:Key="LightChartStyle" TargetType="{x:Type lc:LightningChart}">
        <Setter Property="Background" Value="{DynamicResource RegionBackgroundBrush}" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="IsHitTestVisible" Value="False" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
    </Style>

    <DataTemplate DataType="{x:Type models:AlarmSummaryCard}">
        <Border Margin="6,0,6,12"
                Background="{Binding BackgroundBrush}"
                Style="{StaticResource AlarmContentCardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Border Width="42" Height="5" HorizontalAlignment="Left" Background="{Binding AccentBrush}" CornerRadius="8" />
                <TextBlock Grid.Row="1" Margin="0,14,0,0" Foreground="{StaticResource AlarmMutedBrush}" Text="{Binding Title}" />
                <TextBlock Grid.Row="2" Margin="0,8,0,0" FontSize="28" FontWeight="Bold" Foreground="{StaticResource AlarmTextBrush}" Text="{Binding Value}" />
                <TextBlock Grid.Row="3" Margin="0,8,0,0" Foreground="{StaticResource AlarmMutedBrush}" Text="{Binding Caption}" TextWrapping="Wrap" />
            </Grid>
        </Border>
    </DataTemplate>

    <DataTemplate x:Key="AlarmCompactSummaryCardTemplate" DataType="{x:Type models:AlarmSummaryCard}">
        <Border Margin="4,0,6,8"
                Padding="10,8"
                Background="{Binding BackgroundBrush}"
                BorderBrush="{StaticResource AlarmBorderBrush}"
                BorderThickness="1"
                CornerRadius="14">
            <Grid MinHeight="48">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Width="4"
                        Height="40"
                        VerticalAlignment="Center"
                        Background="{Binding AccentBrush}"
                        CornerRadius="4" />
                <Grid Grid.Column="1" Margin="10,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock VerticalAlignment="Center"
                                   Foreground="{StaticResource AlarmMutedBrush}"
                                   Text="{Binding Title}"
                                   TextTrimming="CharacterEllipsis" />
                        <TextBlock Grid.Column="1"
                                   Margin="8,0,0,0"
                                   VerticalAlignment="Center"
                                   FontSize="22"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource AlarmTextBrush}"
                                   Text="{Binding Value}" />
                    </Grid>
                    <TextBlock Grid.Row="1"
                               Margin="0,3,0,0"
                               FontSize="10"
                               Foreground="{StaticResource AlarmMutedBrush}"
                               Text="{Binding Caption}"
                               TextTrimming="CharacterEllipsis"
                               ToolTip="{Binding Caption}" />
                </Grid>
            </Grid>
        </Border>
    </DataTemplate>


    <DataTemplate DataType="{x:Type models:AlarmFeedItem}">
        <Border Margin="0,0,0,10"
                Padding="12"
                Background="{Binding RowBackground}"
                BorderBrush="{Binding RowBorderBrush}"
                BorderThickness="1"
                CornerRadius="14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Border MinWidth="52" Height="28" Padding="10,0" VerticalAlignment="Top" Background="{Binding LevelBadgeBrush}" CornerRadius="14">
                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="{StaticResource AlarmOnAccentBrush}" Text="{Binding LevelText}" />
                </Border>
                <StackPanel Grid.Column="1" Margin="12,0,12,0">
                    <TextBlock FontWeight="SemiBold" Foreground="{StaticResource AlarmTextBrush}" Text="{Binding Summary}" TextTrimming="CharacterEllipsis" />
                    <TextBlock Margin="0,6,0,0" Foreground="{StaticResource AlarmMutedBrush}" Text="{Binding Message}" TextWrapping="Wrap" />
                    <TextBlock Margin="0,8,0,0" FontSize="12" FontWeight="SemiBold" Foreground="{Binding LevelBadgeBrush}" Text="{Binding ActionText}" />
                </StackPanel>
                <TextBlock Grid.Column="2" VerticalAlignment="Top" Foreground="{StaticResource AlarmMutedBrush}" Text="{Binding TimestampText}" />
            </Grid>
        </Border>
    </DataTemplate>


    <DataTemplate DataType="{x:Type models:AlarmStatisticBarItem}">
        <Border Margin="0,0,0,10"
                Padding="12"
                Background="{StaticResource AlarmPanelBrush}"
                BorderBrush="{StaticResource AlarmBorderBrush}"
                BorderThickness="1"
                CornerRadius="10">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock FontWeight="SemiBold" Foreground="{StaticResource AlarmTextBrush}" Text="{Binding Label}" TextTrimming="CharacterEllipsis" />
                    <Border Grid.Column="1" Padding="8,2" Background="{Binding AccentBrush}" CornerRadius="10">
                        <TextBlock Foreground="{StaticResource AlarmOnAccentBrush}" Text="{Binding CountText}" />
                    </Border>
                </Grid>
                <ProgressBar Height="8" Margin="0,10,0,0" Foreground="{Binding AccentBrush}" Maximum="{Binding Maximum}" Value="{Binding Count}" />
                <TextBlock Margin="0,8,0,0" Style="{StaticResource MutedTextStyle}" Text="{Binding Description}" />
            </StackPanel>
        </Border>
    </DataTemplate>


    <DataTemplate DataType="{x:Type models:AlarmTrendPoint}">
        <Border Width="158" Margin="0,0,12,12" Padding="14" Background="{StaticResource AlarmPanelBrush}" BorderBrush="{StaticResource AlarmBorderBrush}" BorderThickness="1" CornerRadius="14">
            <StackPanel>
                <TextBlock Style="{StaticResource MutedTextStyle}" Text="{Binding Label}" />
                <TextBlock Margin="0,8,0,0" FontSize="22" FontWeight="Bold" Foreground="{StaticResource AlarmTextBrush}" Text="{Binding Count}" />
                <ProgressBar Height="8" Margin="0,10,0,0" Foreground="{Binding AccentBrush}" Maximum="{Binding Maximum}" Value="{Binding Count}" />
                <TextBlock Margin="0,8,0,0" Style="{StaticResource MutedTextStyle}" Text="{Binding Description}" />
            </StackPanel>
        </Border>
    </DataTemplate>


</ResourceDictionary>
