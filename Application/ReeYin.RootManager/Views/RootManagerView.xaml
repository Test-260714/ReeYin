<UserControl x:Class="ReeYin.RootManager.Views.RootManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ReeYin.RootManager.Views"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d" Background="Transparent"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Width="1200" Height="800"
             d:DesignHeight="800" d:DesignWidth="1200">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="54"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--顶部标题栏-->
        <Border Grid.Row="0" BorderThickness="0,0,0,1"
                Background="{DynamicResource TitleBarBrush}"
                BorderBrush="{DynamicResource BorderBrush}" Padding="20,0">
            <TextBlock Text="管理中心"
                       Foreground="{DynamicResource TitleForegroundBrush}"
                       FontSize="17"
                       FontWeight="Medium"
                       VerticalAlignment="Center"/>
        </Border>

        <!--主体：左侧导航 + 右侧内容区-->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!--左侧导航面板-->
            <Border Grid.Column="0" BorderThickness="0,0,1,0"
                    BorderBrush="{DynamicResource BorderBrush}">
                <ListBox ItemsSource="{Binding NavItems}"
                         SelectedItem="{Binding SelectedNavItem, Mode=TwoWay}"
                         DisplayMemberPath="Header"
                         Background="Transparent" BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         FontSize="14" Padding="0,8,0,0"/>
            </Border>

            <!--右侧内容区（Prism Region）-->
            <Border Grid.Column="1" Padding="24,16,24,16">
                <ContentControl prism:RegionManager.RegionName="RootManagerContentRegion"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"/>
            </Border>
        </Grid>

        <!--底部操作栏-->
        <Border Grid.Row="2" BorderThickness="0,1,0,0"
                BorderBrush="{DynamicResource BorderBrush}" Padding="20,10">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal"
                        WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Command="{Binding GeneralCommand}" CommandParameter="取消"
                        Style="{StaticResource GeneralButtonStyle}"
                        Height="34" Width="72" FontSize="14"
                        Content="取消" Margin="0,0,10,0" Cursor="Hand"/>
                <Button Command="{Binding GeneralCommand}" CommandParameter="保存"
                        Style="{StaticResource GeneralButtonStyle}"
                        Height="34" Width="72" FontSize="14"
                        Content="保存" Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
