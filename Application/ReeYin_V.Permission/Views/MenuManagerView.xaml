<UserControl x:Class="ReeYin_V.Permission.Views.MenuManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReeYin_V.Permission.Views"
             xmlns:WxGroupBox="clr-namespace:ReeYin_V.UI.UserControls.WxGroupBox;assembly=ReeYin_V.UI"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:prism="http://prismlibrary.com/" 
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             Height="450" Width="800"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>

    </UserControl.Resources>
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.3*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TreeView ItemsSource="{Binding Menus}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="SelectedItemChanged">
                        <i:InvokeCommandAction Command="{Binding RefreshMenu}" CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=TreeView}}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <TextBlock Text="{Binding Name}">

                        </TextBlock>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
            <TabControl Grid.Column="1">
                <TabControl.ItemContainerStyle>
                    <Style TargetType="{x:Type TabItem}">
                        <Setter Property="Visibility" Value="Collapsed" />
                    </Style>
                </TabControl.ItemContainerStyle>
                <TabItem Header="General">
                    <StackPanel Orientation="Vertical">
                        <WxGroupBox:WxGroupBox Header="菜单设置">
                            <StackPanel>
                                <UniformGrid Columns="4">
                                    <TextBlock Text="名称：" />
                                    <TextBox Text="{Binding CurMenuInfo.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                                    <Label Content="图标：" />
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
                                        <Label HorizontalAlignment="Center" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" VerticalAlignment="Center"  FontSize="20" 
                                        Content="{Binding CurMenuInfo.Icon, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontFamily="{StaticResource Iconfont}"/>
                                        <Button Content="选择图标" HorizontalAlignment="Center" VerticalAlignment="Center"  />
                                    </StackPanel>

                                    <TextBlock Text="类型：" Height="50"/>
                                    <TextBlock Text="{Binding CurMenuInfo.Type, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                    
                                    <TextBlock Text="事件："/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
                                        <TextBlock Text="{Binding CurMenuInfo.Event, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                        <Button Content="选择事件" HorizontalAlignment="Center" VerticalAlignment="Center"  />
                                    </StackPanel>

                                    <TextBlock Text="描述："/>
                                    <TextBox Text="{Binding CurMenuInfo.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    
                                    <TextBlock Text="创建时间："/>
                                    <TextBlock Text="{Binding CurMenuInfo.CreateTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                    <TextBlock Text="修改时间："/>
                                    <TextBlock Text="{Binding CurMenuInfo.UpdateTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </UniformGrid>



                            </StackPanel>
                        </WxGroupBox:WxGroupBox>
                        <WxGroupBox:WxGroupBox Header="操作">
                            <DockPanel>
                                <CheckBox IsChecked="{Binding CurMenuInfo.IsVisiable, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">可见</CheckBox>
                                <CheckBox IsChecked="{Binding CurMenuInfo.CanRead, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">可读</CheckBox>
                                <CheckBox IsChecked="{Binding CurMenuInfo.CanWrite, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">可写</CheckBox>
                            </DockPanel>

                        </WxGroupBox:WxGroupBox>
                    </StackPanel>

                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</UserControl>
