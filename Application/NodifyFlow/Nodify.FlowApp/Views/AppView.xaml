<UserControl x:Class="Nodify.FlowApp.AppView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Nodify.FlowApp"
        Background="{DynamicResource NodifyEditor.BackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}"
        xmlns:Nodify="clr-namespace:ReeYin_V.NodifyManager;assembly=ReeYin_V.NodifyManager"
        xmlns:nodify="https://miroiu.github.io/nodify"
        xmlns:o="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"
        xmlns:sys="clr-namespace:System;assembly=System.Runtime"
        xmlns:FastDrawer="clr-namespace:ReeYin_V.UI.UserControls.FastDrawer;assembly=ReeYin_V.UI"
        xmlns:WxSearchBox="clr-namespace:ReeYin_V.UI.UserControls.WxSearchBox;assembly=ReeYin_V.UI"
        xmlns:WaterDrop="clr-namespace:ReeYin_V.UI.UserControls.WaterDrop;assembly=ReeYin_V.UI"
        xmlns:WxGroupBox="clr-namespace:ReeYin_V.UI.UserControls.WxGroupBox;assembly=ReeYin_V.UI"
        xmlns:Controls="clr-namespace:ReeYin_V.UI.Controls;assembly=ReeYin_V.UI"
        xmlns:share="clr-namespace:ReeYin_V.Share.Prism;assembly=ReeYin_V.Share"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:prism="http://prismlibrary.com/" 
        prism:ViewModelLocator.AutoWireViewModel="False"
        mc:Ignorable="d"
        d:DesignHeight="1080" d:DesignWidth="1920">
    <UserControl.InputBindings>
        <KeyBinding Key="T"
                    Modifiers="Ctrl"
                    Command="{Binding Source={x:Static Nodify:ThemeManager.SetNextThemeCommand}}" />

        <KeyBinding Key="N"
                    Modifiers="Ctrl"
                    Command="{Binding AddEditorCommand}" />
        <KeyBinding Key="W"
                    Modifiers="Ctrl"
                    Command="{Binding CloseEditorCommand}"
                    CommandParameter="{Binding SelectedEditor.Id}"/>
    </UserControl.InputBindings>

    <UserControl.DataContext>
        <local:AppViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <ResourceDictionary x:Name="Resource">

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Nodify;component/Themes/Dark.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/ReeYin_V.NodifyManager;component/UI/Style/Dark.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/ReeYin_V.NodifyManager;component/UI/Style/Generic.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <DataTemplate DataType="{x:Type local:EditorViewModel}">
                <local:EditorView/>
            </DataTemplate>
            <Nodify:BindingProxy x:Key="Proxy" DataContext="{Binding}"/>
            <SolidColorBrush x:Key="PrimaryTextColor" Color="#FFFFFF"/>
            <!--背景样式-->
            <VisualBrush
                x:Key="GridDrawingBrush"
                o:Freeze="True"
                TileMode="Tile"
                Transform="{Binding ViewportTransform, ElementName=Editor}"
                Viewbox="0 0 30 30"
                ViewboxUnits="Absolute"
                Viewport="0 0 30 30"
                ViewportUnits="Absolute">
                <VisualBrush.Visual>
                    <Rectangle
                        Width="1"
                        Height="1"
                        Fill="White" />
                </VisualBrush.Visual>
            </VisualBrush>
        </ResourceDictionary>


    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.7*" />
            <ColumnDefinition Width="4" />
            <ColumnDefinition Width="0.3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <!--<Nodify:TabControlEx ItemsSource="{Binding Editors}"
                             SelectedItem="{Binding SelectedEditor}" 
                             AddTabCommand="{Binding AddEditorCommand}"
                             AutoScrollToEnd="{Binding AutoSelectNewEditor}">
            <Nodify:TabControlEx.ItemContainerStyle>
                <Style TargetType="{x:Type Nodify:TabItemEx}" 
                       BasedOn="{StaticResource {x:Type Nodify:TabItemEx}}">
                    <Setter Property="Header" 
                            Value="{Binding Name}"/>
                    <Setter Property="CloseTabCommand" 
                            Value="{Binding DataContext.CloseEditorCommand ,Source={StaticResource Proxy}}"/>
                    <Setter Property="CloseTabCommandParameter" 
                            Value="{Binding Id}"/>
                </Style>
            </Nodify:TabControlEx.ItemContainerStyle>
        </Nodify:TabControlEx>-->

        <local:NodifyEditorView x:Name="EditorView"
                                DataContext="{Binding GraphViewModel}"
                                Grid.RowSpan="3" />

        <!--ACTIONS-->
        <Border VerticalAlignment="Top" Padding="100 0 0 0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Visibility="Hidden">
                    <Button Command="{Binding GenerateRandomNodesCommand}"
                            Content="GENERATE NODES"
                            ToolTip="Generate nodes using the specified settings."
                            Style="{StaticResource HollowButton}" />
                    <Button Command="{Binding ToggleConnectionsCommand}"
                            Content="{Binding ConnectNodesText}"
                            ToolTip="Will add new connections if Connect Nodes is checked, otherwise it will disconnect nodes."
                            Style="{StaticResource HollowButton}" />
                    <Button Command="{Binding PerformanceTestCommand}"
                            Content="PERFORMANCE TEST"
                            ToolTip="You will encounter rendering performance issues. Try disabling the connections to see the difference."
                            Style="{StaticResource HollowButton}" />
                    <Button Command="{Binding ResetCommand}"
                            Content="RESET PLAYGROUND"
                            ToolTip="Reset the Location, Zoom, Nodes and Connections."
                            Style="{StaticResource HollowButton}" />
                    <Button Click="BringIntoView_Click"
                            Content="BRING INTO VIEW"
                            ToolTip="Bring a random node into view."
                            Style="{StaticResource HollowButton}" />
                    <Button Command="{x:Static nodify:EditorCommands.FitToScreen}"
                            Content="FIT TO SCREEN"
                            ToolTip="Scales the viewport to fit all nodes if that's possible."
                            CommandTarget="{Binding EditorInstance, ElementName=EditorView}"
                            Style="{StaticResource HollowButton}" />
                    <Button Command="{Binding GraphViewModel.CommentSelectionCommand}"
                            Content="COMMENT SELECTION"
                            ToolTip="Creates a comment node containing the selected nodes."
                            Style="{StaticResource HollowButton}" />
                    <Button Click="AnimateConnections_Click"
                            Content="TOGGLE CONNECTIONS ANIMATION"
                            ToolTip="Starts or stops animating the directional arrows on all connections (see DirectionalArrowsCount)"
                            Style="{StaticResource HollowButton}" />
                </StackPanel>

                <Button Style="{StaticResource IconButton}"
                        Content="{StaticResource ThemeIcon}"
                        Command="{Binding Source={x:Static Nodify:ThemeManager.SetNextThemeCommand}}"
                        ToolTip="Change theme"
                        Grid.Column="1" />
            </Grid>
        </Border>

        <Expander Header="{DynamicResource ClickToHideOrShow}"
                  IsExpanded="True"
                  Margin="10"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Bottom">
            <Border MaxWidth="325"
                    MaxHeight="200"
                    CornerRadius="3">
                <Border.Background>
                    <SolidColorBrush Color="Transparent"
                                     Opacity="0.7" />
                </Border.Background>
                <ScrollViewer HorizontalScrollBarVisibility="Disabled" Height="200" Width="300">
                    <WaterDrop:UCWaterDropsButtonGroup x:Name="group1" Width="80" Height="80" HorizontalAlignment="Center" VerticalAlignment="Bottom" Click="UCWaterDropsButtonGroup_Click"></WaterDrop:UCWaterDropsButtonGroup>
                </ScrollViewer>
            </Border>
        </Expander>

        <!--INFORMATION-->
        <Border Grid.Row="1"
                VerticalAlignment="Bottom"
                Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.Resources>
                    <Style TargetType="{x:Type TextBlock}"
                           BasedOn="{StaticResource {x:Type TextBlock}}">
                        <Setter Property="Foreground"
                                Value="{DynamicResource ForegroundBrush}" />
                        <Setter Property="Margin"
                                Value="0 0 15 0" />
                    </Style>
                </Grid.Resources>

                <StackPanel Orientation="Horizontal">
                    <TextBlock ToolTip="The number of selected items.">
                        <TextBlock.Inlines>
                            <Run Text="{DynamicResource SelectedNodes}" />
                            <Run Text=":" />
                            <Run Foreground="YellowGreen"
                                 Text="{Binding GraphViewModel.SelectedNodes.Count, Mode=OneWay}" />
                            <Run Text="/" />
                            <Run Text="{Binding GraphViewModel.Nodes.Count, Mode=OneWay}" />
                        </TextBlock.Inlines>
                    </TextBlock>
                    <TextBlock ToolTip="The number of selected connections.">
                        <TextBlock.Inlines>
                            <Run Text="{DynamicResource SelectedConnections}" />
                            <Run Text=":" />
                            <Run Foreground="YellowGreen"
                                 Text="{Binding GraphViewModel.SelectedConnections.Count, Mode=OneWay}" />
                            <Run Text="/" />
                            <Run Text="{Binding GraphViewModel.Connections.Count, Mode=OneWay}" />
                        </TextBlock.Inlines>
                    </TextBlock>
                </StackPanel>

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <TextBlock ToolTip="The viewport's location.">
                        <TextBlock.Inlines>
                            <Run Text="{DynamicResource Location}" />
                            <Run Text=":" />
                            <Run Foreground="Orange"
                                 Text="{Binding Location.Value, Mode=OneWay, Converter={StaticResource ToStringConverter}, Source={x:Static local:EditorSettings.Instance}}" />
                        </TextBlock.Inlines>
                    </TextBlock>
                    <TextBlock ToolTip="The viewport's size.">
                        <TextBlock.Inlines>
                            <Run Text="{DynamicResource Size}" />
                            <Run Text=":" />
                            <Run Foreground="YellowGreen"
                                 Text="{Binding GraphViewModel.ViewportSize, Mode=OneWay, Converter={StaticResource ToStringConverter}}" />
                        </TextBlock.Inlines>
                    </TextBlock>
                    <TextBlock ToolTip="The viewport's zoom. Not accurate when trying to zoom outside the MinViewportZoom and MaxViewportZoom because of dependency property coercion not updating the binding with the final result.">
                        <TextBlock.Inlines>
                            <Run Text="{DynamicResource Zoom}" />
                            <Run Text=":" />
                            <Run Foreground="DodgerBlue"
                                 Text="{Binding Zoom, Mode=OneWay, Converter={StaticResource ToStringConverter}, Source={x:Static local:EditorSettings.Instance}}" />
                        </TextBlock.Inlines>
                    </TextBlock>
                    <TextBlock ToolTip="The estimated frame rate. (my be buggy)">
                        <TextBlock.Inlines>
                            <Run Text="{DynamicResource FPS}" />
                            <Run Text=": " />
                            <Run Foreground="LawnGreen"
                                 Name="FPSText" />
                        </TextBlock.Inlines>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>
        <GridSplitter Background="{StaticResource BackgroundBrush}" Grid.Column="1" Height="auto" Width="2" HorizontalAlignment="Stretch"/>
        <!--Settings-->
        <Grid Grid.Column="2">
            <Border BorderBrush="{DynamicResource BackgroundBrush}" Padding="10" BorderThickness="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Grid>
                    <StackPanel>
                        <ContentControl prism:RegionManager.RegionName="test"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

    </Grid>
</UserControl>
