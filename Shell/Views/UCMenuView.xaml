<UserControl x:Class="ReeYin_V.Shell.Views.UCMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReeYin_V.Shell.Views"
             mc:Ignorable="d" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Height="80" Margin="-10 0 0 0"
             d:DesignHeight="80" d:DesignWidth="800">
    <Grid Height="80" Margin="0 5 0 0">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="0.2*"/>
        </Grid.ColumnDefinitions>
        
        <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
            </Grid>
            <!--图标-->
            <Image MaxHeight="25" 
           HorizontalAlignment="Center"
           VerticalAlignment="Center"
           Source="{Binding LoginIconPath}"
           MouseRightButtonDown="Image_MouseRightButtonDown"/>

            <GridSplitter Background="{DynamicResource SplitBrush}" Height="20" Width="2" Grid.Column="3"/>

            <TextBlock Text="{DynamicResource Versions}" FontSize="14" Margin="10,0,10,0" VerticalAlignment="Center" HorizontalAlignment="Center"/>

            <GridSplitter Background="{DynamicResource SplitBrush}" Height="20" Width="2" Grid.Column="3"/>

            <!--当前项目-->
            <TextBlock Text="{DynamicResource CurrentProject}" FontSize="14" Margin="10,0,10,0" VerticalAlignment="Center" HorizontalAlignment="Center"/>

            <TextBlock Text="{Binding CurSolutionPath}" FontSize="14" Margin="0,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Center"/>

            <Button Width="35" Height="20" Content="&#xe7b9;" FontSize="14" FontFamily="{StaticResource Iconfont}" BorderThickness="0 0 0 0" Foreground="Black" Background="Transparent" Style="{StaticResource GeneralButtonStyle2}"
                     Margin="0 0 0 0" Command="{Binding GeneralCommand}" CommandParameter="打开"/>
        </WrapPanel>

        <StackPanel Grid.Column="1" Grid.Row="0" HorizontalAlignment="Right" Margin="100 0 0 0">
            <!--<Button Width="35" Height="30" Content="&#xe7b9;" FontSize="16" FontFamily="{StaticResource Iconfont}" BorderThickness="0" Foreground="Black" Background="Transparent" Style="{StaticResource GeneralButtonStyle2}"
                        HorizontalAlignment="Right" Command="{Binding GeneralCommand}" CommandParameter="打开"/>-->
        </StackPanel>
    </Grid>
</UserControl>
