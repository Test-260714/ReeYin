<UserControl x:Class="Custom.XYHD.Views.DetectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mainViews="clr-namespace:ReeYin_V.Main.UC.Views;assembly=ReeYin_V.Main"
             xmlns:WxGroupBox="clr-namespace:ReeYin_V.UI.UserControls.WxGroupBox;assembly=ReeYin_V.UI"
             xmlns:local="clr-namespace:Custom.XYHD.Views"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             FontSize="13"
             Background="#EDF2F6">
    <UserControl.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D8E1EA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="12"/>
        </Style>
        <Style x:Key="GroupContentCardStyle" TargetType="Border" BasedOn="{StaticResource CardStyle}">
            <Setter Property="CornerRadius" Value="0,0,10,10"/>
        </Style>
        <Style x:Key="ModuleButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#C69300"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#B88300"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="MinWidth" Value="92"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="RootBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="#B88300"/>
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="#9D7000"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="#9D7000"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="RootBorder" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CompactModuleButtonStyle" TargetType="Button" BasedOn="{StaticResource ModuleButtonStyle}">
            <Setter Property="Height" Value="30"/>
            <Setter Property="MinWidth" Value="44"/>
            <Setter Property="Padding" Value="10,3"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
        </Style>
        <Style x:Key="SectionTitleTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#1F2937"/>
        </Style>
        <Style x:Key="InfoLabelTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#6B7280"/>
        </Style>
        <Style x:Key="InfoValueTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#111827"/>
        </Style>
        <Style x:Key="ResultBadgeBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#E5E7EB"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="10,4"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="OK">
                    <Setter Property="Background" Value="#DCFCE7"/>
                    <Setter Property="BorderBrush" Value="#86EFAC"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="无缺陷">
                    <Setter Property="Background" Value="#DCFCE7"/>
                    <Setter Property="BorderBrush" Value="#86EFAC"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="NG">
                    <Setter Property="Background" Value="#FEE2E2"/>
                    <Setter Property="BorderBrush" Value="#FCA5A5"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ResultBadgeTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource AncestorType=Border}}" Value="OK">
                    <Setter Property="Foreground" Value="#166534"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource AncestorType=Border}}" Value="无缺陷">
                    <Setter Property="Foreground" Value="#166534"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource AncestorType=Border}}" Value="NG">
                    <Setter Property="Foreground" Value="#B91C1C"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="MiniMetricCardStyle" TargetType="Border" BasedOn="{StaticResource CardStyle}">
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="Padding" Value="12"/>
        </Style>
        <Style x:Key="CompactMetricCardStyle" TargetType="Border" BasedOn="{StaticResource MiniMetricCardStyle}">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="MinHeight" Value="52"/>
            <Setter Property="CornerRadius" Value="8"/>
        </Style>
        <Style x:Key="CompactMetricValueTextStyle" TargetType="TextBlock" BasedOn="{StaticResource InfoValueTextStyle}">
            <Setter Property="FontSize" Value="15"/>
        </Style>
        <Style x:Key="StatusTileStyle" TargetType="Border">
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#D8E1EA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="7"/>
            <Setter Property="Padding" Value="9,6"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="MinHeight" Value="48"/>
        </Style>
        <Style x:Key="StatusTileValueTextStyle" TargetType="TextBlock" BasedOn="{StaticResource InfoValueTextStyle}">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
        <Style x:Key="PreviewMetaTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,16,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="LaneSummaryCardStyle" TargetType="Border">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="MinHeight" Value="52"/>
        </Style>
        <Style x:Key="PreviewSurfaceStyle" TargetType="Border">
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="BorderBrush" Value="#1E293B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="ClipToBounds" Value="True"/>
        </Style>
        <Style x:Key="PreviewPanelStyle" TargetType="Border" BasedOn="{StaticResource CardStyle}">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Background" Value="#FFFFFF"/>
        </Style>
        <Style x:Key="ResultPreviewPanelStyle" TargetType="Border" BasedOn="{StaticResource PreviewPanelStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="OK">
                    <Setter Property="Background" Value="#F0FDF4"/>
                    <Setter Property="BorderBrush" Value="#22C55E"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="无缺陷">
                    <Setter Property="Background" Value="#F0FDF4"/>
                    <Setter Property="BorderBrush" Value="#22C55E"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="NG">
                    <Setter Property="Background" Value="#FEF2F2"/>
                    <Setter Property="BorderBrush" Value="#EF4444"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="PreviewTitleTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="ResultPanelStatusTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource AncestorType=Border}}" Value="OK">
                    <Setter Property="Foreground" Value="#166534"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource AncestorType=Border}}" Value="无缺陷">
                    <Setter Property="Foreground" Value="#166534"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource AncestorType=Border}}" Value="NG">
                    <Setter Property="Foreground" Value="#B91C1C"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="PreviewSubTextStyle" TargetType="TextBlock" BasedOn="{StaticResource PreviewMetaTextStyle}">
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>
        <Style x:Key="StreamStateDotStyle" TargetType="Ellipse">
            <Setter Property="Fill" Value="#9CA3AF"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding StreamState}" Value="Live"><Setter Property="Fill" Value="#16A34A"/></DataTrigger>
                <DataTrigger Binding="{Binding StreamState}" Value="Warn"><Setter Property="Fill" Value="#F59E0B"/></DataTrigger>
                <DataTrigger Binding="{Binding StreamState}" Value="Stale"><Setter Property="Fill" Value="#DC2626"/></DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="76"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="48"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="190"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="带材在线检测" FontSize="18" FontWeight="Bold" Foreground="#1F2937"/>
                    <TextBlock Text="产品模板：待配置" Margin="0,4,0,0" Foreground="#64748B" FontSize="12"/>
                </StackPanel>
                <UniformGrid Grid.Column="1" Columns="7" VerticalAlignment="Center">
                    <Border Style="{StaticResource StatusTileStyle}">
                        <StackPanel>
                            <TextBlock Text="流状态" Style="{StaticResource InfoLabelTextStyle}"/>
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <Ellipse Width="10" Height="10" Margin="0,0,6,0" Style="{StaticResource StreamStateDotStyle}"/>
                                <TextBlock Text="{Binding StreamStatusText}" Style="{StaticResource StatusTileValueTextStyle}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatusTileStyle}">
                        <StackPanel>
                            <TextBlock Text="运行模式" Style="{StaticResource InfoLabelTextStyle}"/>
                            <TextBlock Text="{Binding CurrentModeText}" Margin="0,4,0,0" Style="{StaticResource StatusTileValueTextStyle}"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatusTileStyle}">
                        <StackPanel>
                            <TextBlock Text="当前卷号" Style="{StaticResource InfoLabelTextStyle}"/>
                            <TextBlock Text="{Binding BatchManager.BatchNumberText}" Margin="0,4,0,0" Style="{StaticResource StatusTileValueTextStyle}"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatusTileStyle}">
                        <StackPanel>
                            <TextBlock Text="当前帧" Style="{StaticResource InfoLabelTextStyle}"/>
                            <TextBlock Text="{Binding LastFrameIdText}" Margin="0,4,0,0" Style="{StaticResource StatusTileValueTextStyle}"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatusTileStyle}">
                        <StackPanel>
                            <TextBlock Text="累计米数" Style="{StaticResource InfoLabelTextStyle}"/>
                            <TextBlock Text="{Binding SharedCumulativeMeters, StringFormat='{}{0:F2} m'}" Margin="0,4,0,0" Style="{StaticResource StatusTileValueTextStyle}"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatusTileStyle}">
                        <StackPanel>
                            <TextBlock Text="速度" Style="{StaticResource InfoLabelTextStyle}"/>
                            <TextBlock Text="待接入" Margin="0,4,0,0" Style="{StaticResource StatusTileValueTextStyle}"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatusTileStyle}" Margin="0">
                        <StackPanel>
                            <TextBlock Text="当前缺陷" Style="{StaticResource InfoLabelTextStyle}"/>
                            <TextBlock Text="{Binding LastDefectCount, StringFormat='{}{0} 个'}" Margin="0,4,0,0" Style="{StaticResource StatusTileValueTextStyle}" Foreground="#B91C1C"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                    <Button Style="{StaticResource ModuleButtonStyle}" Content="换卷" Command="{Binding ChangeBatchCommand}"/>
                    <Button Style="{StaticResource ModuleButtonStyle}" Content="保存缺陷" Command="{Binding SaveDefectsCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="0" Margin="0,10,0,10">
            <TabControl Background="Transparent" BorderThickness="0" Style="{StaticResource TabControlWithUnderLineStyle}" SelectedIndex="{Binding SelectedMainTabIndex, Mode=TwoWay}">
                <TabItem Header="主界面" Width="96" Style="{StaticResource TabItemExWithUnderLineStyle}">
                    <Grid Margin="12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Padding="10,8" Background="#F8FAFC" BorderBrush="#D8E1EA" BorderThickness="1" CornerRadius="8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="左右路方向" Style="{StaticResource SectionTitleTextStyle}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                <TextBlock Grid.Column="1" Text="{Binding FieldOrientationSummary}" Foreground="#334155" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding FrameIntervalText, StringFormat='帧间隔 {0}'}" Style="{StaticResource PreviewMetaTextStyle}"/>
                                    <TextBlock Text="{Binding LastFrameTimeText, StringFormat='最近更新 {0}'}" Style="{StaticResource PreviewMetaTextStyle}" Margin="0"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2.35*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Tag="{Binding LastResult}" Style="{StaticResource ResultPreviewPanelStyle}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="整帧预览" Style="{StaticResource PreviewTitleTextStyle}" Margin="0,0,14,0"/>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding LastFrameIdText, StringFormat='当前帧 {0}'}" Style="{StaticResource PreviewSubTextStyle}"/>
                                            <TextBlock Text="{Binding ImageSizeText, StringFormat='图像 {0}'}" Style="{StaticResource PreviewSubTextStyle}"/>
                                            <TextBlock Text="{Binding LastFramePieceCount, StringFormat='本帧总片 {0}'}" Style="{StaticResource PreviewSubTextStyle}"/>
                                            <TextBlock Text="{Binding LastFrameNgPieceCount, StringFormat='本帧NG {0}'}" Style="{StaticResource PreviewSubTextStyle}"/>
                                            <TextBlock Text="{Binding Model.TotalCount, StringFormat='累计总片 {0}'}" Style="{StaticResource PreviewSubTextStyle}"/>
                                            <TextBlock Text="{Binding Model.NGCount, StringFormat='累计NG {0}'}" Style="{StaticResource PreviewSubTextStyle}"/>
                                            <TextBlock Text="{Binding SharedRangeSummary}" Style="{StaticResource PreviewSubTextStyle}"/>
                                        </StackPanel>
                                        <Border Grid.Column="2" Tag="{Binding LastResult}" Style="{StaticResource ResultBadgeBorderStyle}" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding LastResult}" Style="{StaticResource ResultBadgeTextStyle}"/>
                                        </Border>
                                    </Grid>
                                    <Border Grid.Row="1" MinHeight="560" Margin="0,10,0,0" Style="{StaticResource PreviewSurfaceStyle}">
                                        <Grid>
                                            <Image Source="{Binding DisplayImage}" Stretch="Uniform"/>
                                            <TextBlock Text="等待相机首帧"
                                                       Foreground="#94A3B8"
                                                       FontSize="14"
                                                       FontWeight="SemiBold"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding DisplayImage}" Value="{x:Null}">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Grid>
                                    </Border>
                                </Grid>
                            </Border>

                            <Grid Grid.Column="2">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="12"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" Tag="{Binding Path1Result}" Style="{StaticResource ResultPreviewPanelStyle}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Path1Header}" Style="{StaticResource PreviewTitleTextStyle}"/>
                                            <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                                <TextBlock Text="{Binding Path1Result}" Style="{StaticResource ResultPanelStatusTextStyle}"/>
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                                    <TextBlock Text="{Binding Path1PieceCount, StringFormat='累计计片 {0}'}" Foreground="#64748B" FontSize="12" Margin="0,0,8,0"/>
                                                    <TextBlock Text="{Binding Path1NgPieceCount, StringFormat='累计NG {0}'}" Foreground="#64748B" FontSize="12" Margin="0,0,8,0"/>
                                                    <TextBlock Text="{Binding Path1DefectCount, StringFormat='缺陷 {0}'}" Foreground="#64748B" FontSize="12"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Grid>
                                        <TextBlock Grid.Row="1" Margin="0,4,0,8" Text="{Binding LeftPreviewOrientationText}" Foreground="#64748B" FontSize="12" TextTrimming="CharacterEllipsis"/>
                                        <Border Grid.Row="2" MinHeight="250" Style="{StaticResource PreviewSurfaceStyle}">
                                            <Grid>
                                                <Image Source="{Binding LeftDisplayImage}" Stretch="Uniform"/>
                                                <TextBlock Text="等待路1图像"
                                                           Foreground="#94A3B8"
                                                           FontSize="13"
                                                           FontWeight="SemiBold"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding LeftDisplayImage}" Value="{x:Null}">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Grid>
                                        </Border>
                                    </Grid>
                                </Border>

                                <Border Grid.Row="2" Tag="{Binding Path2Result}" Style="{StaticResource ResultPreviewPanelStyle}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Path2Header}" Style="{StaticResource PreviewTitleTextStyle}"/>
                                            <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                                <TextBlock Text="{Binding Path2Result}" Style="{StaticResource ResultPanelStatusTextStyle}"/>
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                                    <TextBlock Text="{Binding Path2PieceCount, StringFormat='累计计片 {0}'}" Foreground="#64748B" FontSize="12" Margin="0,0,8,0"/>
                                                    <TextBlock Text="{Binding Path2NgPieceCount, StringFormat='累计NG {0}'}" Foreground="#64748B" FontSize="12" Margin="0,0,8,0"/>
                                                    <TextBlock Text="{Binding Path2DefectCount, StringFormat='缺陷 {0}'}" Foreground="#64748B" FontSize="12"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Grid>
                                        <TextBlock Grid.Row="1" Margin="0,4,0,8" Text="{Binding RightPreviewOrientationText}" Foreground="#64748B" FontSize="12" TextTrimming="CharacterEllipsis"/>
                                        <Border Grid.Row="2" MinHeight="250" Style="{StaticResource PreviewSurfaceStyle}">
                                            <Grid>
                                                <Image Source="{Binding RightDisplayImage}" Stretch="Uniform"/>
                                                <TextBlock Text="等待路2图像"
                                                           Foreground="#94A3B8"
                                                           FontSize="13"
                                                           FontWeight="SemiBold"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding RightDisplayImage}" Value="{x:Null}">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Grid>
                                        </Border>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Grid>

                    </Grid>
                </TabItem>

                <TabItem Header="设置" Width="96" Style="{StaticResource TabItemExWithUnderLineStyle}">
                    <Grid Margin="12">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="0.42*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="0.58*"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <WxGroupBox:WxGroupBox Header="图像存储"><Border Style="{StaticResource GroupContentCardStyle}"><StackPanel><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="40"/></Grid.ColumnDefinitions><TextBox Height="30" VerticalContentAlignment="Center" Text="{Binding Model.SavePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/><Button Grid.Column="1" Content="..." Style="{StaticResource CompactModuleButtonStyle}" Command="{Binding GeneralCommand}" CommandParameter="选择保存路径"/></Grid><StackPanel Orientation="Horizontal" Margin="0,12,0,0"><CheckBox Content="保存 OK 图" IsChecked="{Binding Model.SaveOKImages, Mode=TwoWay}" Margin="0,0,16,0"/><CheckBox Content="保存 NG 图" IsChecked="{Binding Model.SaveNGImages, Mode=TwoWay}"/></StackPanel></StackPanel></Border></WxGroupBox:WxGroupBox>
                            <WxGroupBox:WxGroupBox Header="现场方向" Margin="0,12,0,0"><Border Style="{StaticResource GroupContentCardStyle}"><StackPanel><CheckBox Content="左右路交换" IsChecked="{Binding Model.SwapLeftRightPaths, Mode=TwoWay}" Margin="0,0,0,8"/><CheckBox Content="左路 X 镜像" IsChecked="{Binding Model.LeftPathXMirror, Mode=TwoWay}" Margin="0,0,0,8"/><CheckBox Content="右路 X 镜像" IsChecked="{Binding Model.RightPathXMirror, Mode=TwoWay}"/><Border Margin="0,10,0,0" Padding="8,6" Background="#F3F6FA" BorderBrush="#D8E1EA" BorderThickness="1" CornerRadius="4"><TextBlock Text="{Binding FieldOrientationSummary}" TextWrapping="Wrap" FontSize="12" Foreground="#334155"/></Border></StackPanel></Border></WxGroupBox:WxGroupBox>
                        </StackPanel>
                        <mainViews:LogView Grid.Column="2"/>
                    </Grid>
                </TabItem>

            </TabControl>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="12,6">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Style="{StaticResource ModuleButtonStyle}" Content="取消" Command="{Binding GeneralCommand}" CommandParameter="取消"/>
                    <Button Style="{StaticResource ModuleButtonStyle}" Content="确认" Command="{Binding GeneralCommand}" CommandParameter="确认"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
