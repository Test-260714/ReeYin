<Window x:Class="ReeYin.Hardware.Sensor.TronSight2.CustomUI.Views.TronSight2ThicknessCorrectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:WxGroupBox="clr-namespace:ReeYin_V.UI.UserControls.WxGroupBox;assembly=ReeYin_V.UI"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        Title="厚度修正"
        Width="860"
        Height="450"
        WindowStartupLocation="CenterOwner"
        Background="#F4F4F4"
        FontSize="14">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <WxGroupBox:WxGroupBox Header="厚度修正" Margin="0 0 0 10">
                <DataGrid Margin="8"
                      Height="320"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="All"
                      RowHeaderWidth="0"
                      ScrollViewer.VerticalScrollBarVisibility="Disabled"
                      ItemsSource="{Binding ThicknessCorrectionItems}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="厚度编号" Width="90" IsReadOnly="True" Binding="{Binding DisplayName}"/>
                    <DataGridTextColumn Header="材料名称" Width="100" IsReadOnly="True" Binding="{Binding MaterialName}"/>
                    <DataGridTextColumn Header="1285nm" Width="110" IsReadOnly="True" Binding="{Binding RefractiveIndex1285, StringFormat=F6}"/>
                    <DataGridTextColumn Header="1310nm" Width="110" IsReadOnly="True" Binding="{Binding RefractiveIndex1310, StringFormat=F6}"/>
                    <DataGridTextColumn Header="1335nm" Width="110" IsReadOnly="True" Binding="{Binding RefractiveIndex1335, StringFormat=F6}"/>
                    <DataGridTemplateColumn Header="从数据库选择" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Width="80"
                                        Height="30"
                                        Click="SelectRefractiveButton_Click"
                                        Style="{StaticResource GeneralButtonStyle}"
                                        Content="选择"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="修正系数" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <hc:NumericUpDown Height="30"
                                                  Minimum="0"
                                                  Maximum="999999"
                                                  DecimalPlaces="3"
                                                  Increment="0.001"
                                                  LostFocus="CorrectionFactor_LostFocus"
                                                  Value="{Binding CorrectionFactor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </WxGroupBox:WxGroupBox>

        <StackPanel Grid.Row="1"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Width="100"
                    Height="34"
                    IsCancel="True"
                    Style="{StaticResource GeneralButtonStyle}"
                    Content="关闭"/>
        </StackPanel>
    </Grid>
</Window>
