<UserControl x:Class="ReeYin.Hardware.LightController.Rsee.CustomUI.Views.RseeLightControllerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             Width="1220" Height="720"
             Background="#F5F5F5"
             FontSize="14"
             d:DesignHeight="720" d:DesignWidth="1220">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="68"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="52"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,0" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="锐视 PM-S-48W200-4T" FontSize="16" FontWeight="SemiBold" Foreground="#333" Margin="0,0,24,0"/>
                    <Border CornerRadius="10" Background="#F3F7F4" Padding="10,4">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="状态：" Foreground="#666"/>
                            <TextBlock FontWeight="SemiBold">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="未连接"/>
                                        <Setter Property="Foreground" Value="#D32F2F"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                                <Setter Property="Text" Value="已连接"/>
                                                <Setter Property="Foreground" Value="#2E7D32"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="4通道脉宽 0-999us" Foreground="#666" Margin="0,0,18,0"/>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="#1976D2" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16,0" Margin="0,0,0,12">
            <Grid VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="240"/>
                    <ColumnDefinition Width="54"/>
                    <ColumnDefinition Width="130"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="IP地址" VerticalAlignment="Center" Foreground="#666"/>
                <TextBox Grid.Column="1" Text="{Binding IP}" Height="32" VerticalContentAlignment="Center" Padding="8,0"/>
                <TextBlock Grid.Column="2" Text="端口" VerticalAlignment="Center" Margin="16,0,0,0" Foreground="#666"/>
                <TextBox Grid.Column="3" Text="{Binding Port}" Height="32" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                <StackPanel Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="连接" Width="88" Height="34" Margin="0,0,8,0" Style="{StaticResource GeneralButtonStyle}" Command="{Binding GeneralCommand}" CommandParameter="Connect"/>
                    <Button Content="断开" Width="88" Height="34" Margin="0,0,8,0" Style="{StaticResource GeneralButtonStyle}" Command="{Binding GeneralCommand}" CommandParameter="Disconnect"/>
                    <Button Content="读取全部" Width="108" Height="34" Style="{StaticResource GeneralButtonStyle}" Command="{Binding GeneralCommand}" CommandParameter="Refresh"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,0,18,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="全局脉宽" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,10"/>
                        <TextBlock Text="应用到全部通道，范围 0-999 us" Foreground="#666" FontSize="12" Margin="0,0,0,8"/>
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="82"/>
                            </Grid.ColumnDefinitions>
                            <Slider Minimum="0" Maximum="999" Value="{Binding GlobalPulseWidthUs}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <TextBox Grid.Column="1" Text="{Binding GlobalPulseWidthUs}" Height="32" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
                        </Grid>
                        <Button Content="应用到全部通道" Height="34" Margin="0,0,0,8" Style="{StaticResource GeneralButtonStyle}" Command="{Binding GeneralCommand}" CommandParameter="ApplyAllPulseWidth"/>
                        <Button Content="全部关闭" Height="34" Style="{StaticResource GeneralButtonStyle}" Command="{Binding GeneralCommand}" CommandParameter="AllOff"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="触发与周期" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,10"/>
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Content="外触发 L" Height="34" Style="{StaticResource GeneralButtonStyle}" Command="{Binding GeneralCommand}" CommandParameter="ExternalTrigger"/>
                            <Button Grid.Column="2" Content="内触发 H" Height="34" Style="{StaticResource GeneralButtonStyle}" Command="{Binding GeneralCommand}" CommandParameter="InternalTrigger"/>
                        </Grid>
                        <TextBlock Text="内触发周期 10-999 ms" Foreground="#666" FontSize="12" Margin="0,0,0,6"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="70"/>
                                <ColumnDefinition Width="70"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding InternalTriggerCycleMs}" Height="32" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Margin="0,0,8,0"/>
                            <Button Grid.Column="1" Content="设置" Height="32" Margin="0,0,8,0" Style="{StaticResource GeneralButtonStyle}" Command="{Binding GeneralCommand}" CommandParameter="SetCycle"/>
                            <Button Grid.Column="2" Content="读取" Height="32" Style="{StaticResource GeneralButtonStyle}" Command="{Binding GeneralCommand}" CommandParameter="ReadCycle"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="16">
                    <StackPanel>
                        <TextBlock Text="设备能力" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,10"/>
                        <TextBlock Text="TCP 网口：192.168.1.252:8234" Foreground="#666" Margin="0,0,0,6"/>
                        <TextBlock Text="A/B/C/D 四路脉宽独立设置" Foreground="#666" Margin="0,0,0,6"/>
                        <TextBlock Text="支持内触发、外触发切换" Foreground="#666" Margin="0,0,0,6"/>
                        <TextBlock Text="支持内触发周期设置与读取" Foreground="#666"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="通道脉宽" FontSize="15" FontWeight="SemiBold" Foreground="#333"/>
                        <TextBlock Grid.Column="1" Text="读写通道脉宽，设置成功后控制器返回 A/B/C/D" Foreground="#999" FontSize="12" VerticalAlignment="Center"/>
                    </Grid>
                    <DataGrid Grid.Row="1" AutoGenerateColumns="False" ItemsSource="{Binding ChannelList}" CanUserAddRows="False" HeadersVisibility="Column" RowHeight="54" FontSize="14" GridLinesVisibility="Horizontal">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="通道" Binding="{Binding ChannelIndex}" Width="80" IsReadOnly="True"/>
                            <DataGridTemplateColumn Header="脉宽(us)" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="112"/>
                                            </Grid.ColumnDefinitions>
                                            <Slider Minimum="0" Maximum="999" Value="{Binding PulseWidthUs, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="6,0,14,0" VerticalAlignment="Center"/>
                                            <TextBox Grid.Column="1" Text="{Binding PulseWidthUs, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="32" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridCheckBoxColumn Header="开启" Binding="{Binding IsOn}" Width="90" IsReadOnly="True"/>
                            <DataGridTemplateColumn Header="操作" Width="190">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="设置" Width="78" Height="32" Margin="0,0,8,0" Style="{StaticResource GeneralButtonStyle}" Command="{Binding DataContext.SetChannelCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                            <Button Content="读取" Width="78" Height="32" Style="{StaticResource GeneralButtonStyle}" Command="{Binding DataContext.GetChannelCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,12,0,0">
            <Button Content="确定" Width="90" Height="34" Margin="0,0,10,0" Style="{StaticResource GeneralButtonStyle}" Command="{Binding GeneralCommand}" CommandParameter="OK"/>
            <Button Content="取消" Width="90" Height="34" Style="{StaticResource GeneralButtonStyle}" Command="{Binding GeneralCommand}" CommandParameter="Cancel"/>
        </StackPanel>
    </Grid>
</UserControl>

